import React, { FC } from 'react';
import { Page } from '../Page';

export const LoginPage: FC = () => {
    return (
        <Page>
            <Page.Content>

            </Page.Content>
        </Page>
    )
}
